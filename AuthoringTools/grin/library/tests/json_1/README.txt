

This directory contains a unit test of JsonIO.  I didn't write
an ant file for it; the javac and java commands to run it are
pretty obvious.

In the future, this could be integrated into a set of unit tests
that get run automatically.
